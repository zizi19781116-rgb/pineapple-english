# 交付检查记录

核对日期：2026-09-07。

## 已实际执行

- 创建完整 Xcode 工程，共 90 个文件（打包前统计）。
- Swift 文件 **64 个**，共 **4593 行**；使用 tree-sitter 0.26.0 / tree-sitter-swift 0.7.3 完成语法解析，未发现 ERROR 或缺失节点。
- **23 项静态与工程检查通过**：独立 OpenStep 解析、全部工程对象引用、源文件编译阶段覆盖、共享 Scheme 的目标引用、Info / Privacy plist、资源 JSON、图标尺寸、导入样例、无强制 try / fatalError、无内嵌真实 Key、CloudKit 关闭等。
- Mac 验证脚本通过 Bash 语法检查（`bash -n`）；工程生成器通过 Python AST 语法检查，并已实际生成工程。
- 测试源码中共有 **79 个测试方法**。这是已编写的数量，**不是已运行通过的数量**。
- 完整检查明细保存在本目录 `检查结果.json`。

## 没有执行，也没有宣称通过

| 检查 | 原因 / 当前状态 |
|---|---|
| Swift 编译器类型检查 / swift test | 当前 Windows 环境没有 Swift 编译器 |
| Xcode 编译、签名、模拟器运行 | 当前没有 Mac、Xcode 或 Apple SDK |
| XCTest / XCUITest 实际运行 | 需在 Mac 上执行，不以解析检查代替 |
| iPad / iPhone 真机、外接键盘、1/2 与 2/3 分屏 | 需真实设备与系统 UI 验证 |
| DeepSeek 真实联网测试 | 没有提供或使用用户的真实 API Key |
| 两设备完整迁入 / 迁出 | 需实际安装后执行 |
| 长期大词库性能与故障注入 | 已给出测试清单，尚未做真机压力测试 |

## 下一步

在 Mac 上打开 `菠萝单词.xcodeproj`，或运行 `bash 工具/验证Mac.command`。先完成真实编译和自动测试，再按《测试与验收.md》检查语音、分屏、AI、备份恢复与存储故障。项目未配置开发团队，不包含签名证书或已签名 IPA。

本次没有伪造应用截图、学习统计、完整教材或测试通过日志。Collins 接口明确标记为待授权适配，未配置时隐藏。
